[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "get_movies_project"
version = "0.1.0"
dependencies = [
    "Flask>=2.3.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["backend*"]
